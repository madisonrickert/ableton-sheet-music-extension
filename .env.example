# Copy to .env and set for your machine. Used by `extensions-cli run` (npm start).
# Path to Ableton Live's Extension Host module (the .app is fine on macOS).
EXTENSION_HOST_PATH=/Applications/Ableton Live 12 Suite.app
